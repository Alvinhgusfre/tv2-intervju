# Setup for testing

1) Make sure you have node installed and up to date

2) Run
```bash
pnpm install
```
3) create a .env file in the root folder (video-carousel-thing)

4) Add this variable along with your OMDB key in .env 
```bash
OMDB_API_KEY=yourKey
```

5) Run
```bash
pnpm run dev
```
